# Committed migrations live here.
#
# This directory is intentionally empty until the database is baselined —
# see docs/MIGRATIONS.md. After `npx supabase db pull`, the baseline migration
# (and every migration after it) lives in this folder and is the source of
# truth for the schema going forward.
